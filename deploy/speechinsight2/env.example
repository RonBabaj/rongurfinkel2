# Copy to .env for local runs. In Render, set these in the Web Service → Environment.
# No secrets in repo.

PORT=8080

# Example: Whisper or API keys (set in Render dashboard)
# OPENAI_API_KEY=
# ASPNETCORE_ENVIRONMENT=Production
